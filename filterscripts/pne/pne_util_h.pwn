forward Float:PNE_frandom(Float:max);
forward Float:PNE_frandom2(Float:min, Float:max);
forward Float:GetAngleDif(Float:firstAngle, Float:secondAngle);

enum (+=1)
{
	WEAPON_TYPE_MELEE,
	WEAPON_TYPE_OTHER
};